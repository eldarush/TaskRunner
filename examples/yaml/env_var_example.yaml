# Example demonstrating environment variable substitution
# This task shows how to use environment variables in task configurations

- name: env_var_example
  type: log
  config:
    message: "Environment variable EXAMPLE_VAR is: ${EXAMPLE_VAR}"