# Example demonstrating the HttpGetTask plugin
# This task makes an HTTP GET request to a URL and prints the status code

- name: http_get_example
  type: http_get
  config:
    url: "https://httpbin.org/status/200"